<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===== Izin yang benar-benar dipakai fitur nyata di app ini ===== -->

    <!-- Wajib untuk semua koneksi jaringan (ping, dns test, ads, firebase) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Untuk Game Detector: melihat daftar app terinstal (kebijakan Play
         mengizinkan ini untuk kategori app booster/launcher/antivirus,
         wajib dijelaskan di Play Console Data Safety form) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Untuk FPS HUD floating overlay (legal, user harus approve manual
         lewat halaman Settings sistem, tidak otomatis) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Untuk baca level baterai & suhu (broadcast resmi, tidak berbahaya) -->
    <uses-permission android:name="android.permission.BATTERY_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Untuk Storage Cleaner / Screenshot Manager / Duplicate Finder
         (Android 13+ menggunakan izin granular, bukan lagi READ_EXTERNAL_STORAGE) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Notifikasi (Android 13+ wajib runtime permission) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- In-App Billing (Premium) -->
    <uses-permission android:name="com.android.vending.BILLING" />

    <!-- Vibration untuk haptic feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- TIDAK ADA izin berikut karena TIDAK diimplementasikan (butuh root/API privat):
         - WRITE_SECURE_SETTINGS (ubah animation scale sistem)
         - KILL_BACKGROUND_PROCESSES lintas app (tidak berfungsi sejak Android 5+)
         - Akses raw socket ICMP (butuh CAP_NET_RAW/root) -->

    <application
        android:label="Game Turbo X"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- AdMob App ID: GANTI dengan App ID AdMob asli milik Anda sebelum build rilis.
             Nilai di bawah adalah TEST APP ID resmi dari Google, aman dipakai
             selama development agar tidak melanggar kebijakan AdMob. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>

    </application>

    <!-- Wajib Android 11+ agar QUERY_ALL_PACKAGES di atas benar-benar berfungsi
         untuk deteksi game (package visibility) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

</manifest>
